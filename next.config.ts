import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com",'source.unsplash.com', "cdn.pixabay.com", "images.pexel.com", "i.pravatar.cc"],
  },
};

export default nextConfig;
